stages:
    - build
    - deploy

before_script:
    - mix local.hex --force
    - mix local.rebar --force

variables:
    APPLICATION_NAME: astarte_data_updater_plant

mix:test-ci:
    image: elixir:1.5
    stage: build
    services:
        - scylladb/scylla:latest
        - rabbitmq:3.6
    script:
        - export MIX_ENV=test
        - mix deps.get --force
        - mix deps.compile --force
        - mix compile --force
        - mix coveralls
    cache:
        paths:
            - deps
    except:
        - branches@Astarte-NG/astarte_data_updater_plant

mix:build:
    image: elixir:1.5
    stage: build
    services:
        - scylladb/scylla:latest
        - rabbitmq:3.6
    script:
        - export MIX_ENV=test
        - mix deps.get --force
        - mix deps.compile --force
        - mix compile --force
        - mix coveralls
        - export MIX_ENV=dev
        - mix release
    cache:
        paths:
            - deps
    only:
        - master@Astarte-NG/astarte_data_updater_plant

mix:release-build:
    image: elixir:1.5
    stage: build
    services:
        - scylladb/scylla:latest
        - rabbitmq:3.6
    script:
        - export MIX_ENV=prod
        - mix deps.get --force
        - mix deps.compile --force
        - mix compile --force
        - mix test
        - mix release
    cache:
        paths:
            - deps
    only:
        - tags@Astarte-NG/astarte_data_updater_plant

docker:snapshot-container:
    image: docker:latest
    before_script:
        - docker info
        - docker login -u gitlab-ci-token -p $CI_BUILD_TOKEN registry.ispirata.com
    services:
        - docker:stable-dind
    stage: deploy
    only:
        - master@Astarte-NG/astarte_data_updater_plant
    script:
        - docker build -t registry.ispirata.com/astarte-ng/astarte_data_updater_plant:snapshot .
        - docker push registry.ispirata.com/astarte-ng/astarte_data_updater_plant:snapshot
